<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dzkj.mapper.alarm_setting.AlarmInfoMapper">
    <select id="getMaxLevel" resultType="com.dzkj.entity.alarm_setting.AlarmInfo">
        select project_id, max(alarm_level) as alarmLevel from alarm_info
        where project_id in
        <foreach collection="list" index="index" item="item"
                 separator="," open="(" close=")">
            #{item}
        </foreach>
        group by project_id
    </select>
</mapper>
