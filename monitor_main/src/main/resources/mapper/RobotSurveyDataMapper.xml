<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dzkj.mapper.survey.RobotSurveyDataMapper">

    <select id="getLatestSurveyData" resultType="com.dzkj.entity.survey.RobotSurveyData">
        select raw_data
        from robot_survey_data
        where mission_id = #{missionId} and recycle_num = (
            select max(recycle_num) from robot_survey_data where mission_id = #{missionId}
        )
    </select>

</mapper>
